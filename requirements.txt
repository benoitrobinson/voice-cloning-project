# Local zero-shot voice cloning (pulls torch, torchaudio, transformers)
chatterbox-tts==0.1.7

# chatterbox's watermarker imports pkg_resources, which ships with setuptools
# but is removed in 81+. Without this the model fails to load.
setuptools<81

# Reference-audio cleanup
librosa>=0.11.0
noisereduce>=3.0.3
soundfile>=0.14.0
scipy>=1.15.0
numpy>=1.26,<2

# Recording
sounddevice>=0.5.5

# Tests
pytest>=8.0

# ffmpeg is required for MP3 output and loudness normalization:
#   brew install ffmpeg
